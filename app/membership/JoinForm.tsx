// components/membership/JoinForm.tsx

import React from 'react';

export default function JoinForm() {
  // We'll add state, handlers, and UI in Bolt next!
  return (
    <form>
      {/* Membership form fields and logic go here */}
    </form>
  );
}
